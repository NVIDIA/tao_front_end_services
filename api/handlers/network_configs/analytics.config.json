{
    "api_params": {
        "dataset_type":"object_detection",
        "actions": ["analyze", "validate"],
        "formats":["default"],
        "image":"tao-ds",
        "spec_backend":"yaml",
        "classwise":"False",
        "actions_pipe":{
            "analyze":"data_services",
            "validate":"data_services"
        }
    },
    
    "cli_params": {
        "analyze": {
            "experiment_spec_file":"experiment_spec"
        },
        "validate": {
            "experiment_spec_file":"experiment_spec"
        }
    },
    
    "spec_params":{
        "analyze": {
            "data.output_dir":"output_dir",
            "results_dir":"output_dir"
        },
        "validate": {
            "data.output_dir":"output_dir",
            "results_dir":"output_dir"
        }
    }
}
